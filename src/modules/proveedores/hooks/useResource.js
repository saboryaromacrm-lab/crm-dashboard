/* El mismo hook genérico de datos de Gastos: pedir, recargar, estado de carga.
 * Re-export y no copia — un fix allá sirve acá. */
export { useResource } from '@modules/gastos/hooks/useResource.js';
