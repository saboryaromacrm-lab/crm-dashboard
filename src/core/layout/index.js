/**
 * Public surface of the layout package. Import from '@core/layout' rather than
 * reaching into internal files, so the internal structure can change freely.
 */
export { MainLayout } from './MainLayout.jsx';
